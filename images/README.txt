Drop your photos and logo in this folder, then reference them in the HTML
(replace the dashed "photo-slot" placeholder blocks with <img src="images/your-file.jpg" alt="...">).

Suggested files to add:
- logo.svg or logo.png — replace the inline SVG mark in the header if you have a real campaign logo
- library-exterior.jpg
- rally-crowd.jpg
- petition-signing.jpg

Keep images under ~500KB each (resize/compress before upload) so the site stays fast on mobile.
